<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link href="https://fonts.googleapis.com/css?family=IBM+Plex+Sans:400,400i,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="flex-column min-100-vh">
  <nav class="navbar">
    <ul class="flex-row">
      <li class="li-inline nav-item"><a href="/">Home</a></li>
      <li class="li-inline nav-item"><a href="/dashboard">Dashboard</a></li>
      <li class="li-inline nav-item"><a href="#" id="logout-link">Logout</a></li>
    </ul>
  </nav>
  <header class="hero">
    <h1 class="app-title"><a href="/dashboard">Your Dashboard</a></h1>
  </header>

  <main class="col-auto">
    {{{body}}}
  </main>

  <footer class="footer flex-row justify-center">
    <a href="/dashboard/new">➕ New Post</a>
  </footer>
</body>

<script src="/js/logout.js"></script>

</html>

{{!-- <div>
    <h1>My Dashboard</h1>
    {{#if posts.length}}
    <h3>Posts</h3>
    {{#each posts as |post|}}
    <div>
        <div>
            {{> post-info post}}
            <a href="/post/{{post.id}}">{{post.title}}</a>
            <p>Posted by {{post.user.username}} on {{format_date post.created_at}}</p>
            <div>
                <button class="button"><a href="/dashboard/edit/{{post.id}}">Edit Post</a></button>
            </div>
        </div>
    </div>
    {{/each}}
    {{/if}}

    {{> comments post.comments}}

    {{#if loggedIn}}
    <div>
        <button class="button"><a href="/dashboard/new">New Post</a></button>
    </div>
    {{else}}
    <p>No posts to display!</p>
    {{/if}}
</div>

<script src="/js/new.js"></script>
<script src="/js/comment.js"></script> --}}